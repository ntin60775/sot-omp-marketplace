# GitMark derived search index — regenerated from md
.gitmark/

# GitMark derived HTML overview/graph — regenerated
docs-map.html
*-map.html

# Python bytecode
__pycache__/
*.pyc

# Agent HTML artifacts — ephemeral
.artifacts/

# Session-ephemeral artifacts — never KB knowledge
.scratch/

# Установленный плагин — результат `omp plugin install --scope project ontoship@sot-omp-marketplace`
.omp/

# Реестр потребителей этой машины: абсолютные пути и состав проектов не коммитятся.
# Схема реестра — schemas/consumer-registry.schema.json, она коммитится.
.consumers.json

.DS_Store
